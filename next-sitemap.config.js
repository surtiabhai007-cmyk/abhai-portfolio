/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://yourdomain.com', // ⚡ Replace with your live domain (or Vercel domain)
  generateRobotsTxt: true, // generate robots.txt
  sitemapSize: 5000,
  changefreq: 'monthly',
  priority: 0.7,
};
